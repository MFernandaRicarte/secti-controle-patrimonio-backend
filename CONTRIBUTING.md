# Contribuindo

1. Crie uma branch a partir de `dev`: `git checkout -b feature/<nome>`
2. Commits padronizados: `tipo(escopo): mensagem` (ex.: feat(users): cria listagem)
3. Abra PR para `dev` com descrição e checklist.
